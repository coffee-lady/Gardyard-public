import { FliterOrdersPipe } from './fliter-orders.pipe';

describe('FliterOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new FliterOrdersPipe();
    expect(pipe).toBeTruthy();
  });
});
