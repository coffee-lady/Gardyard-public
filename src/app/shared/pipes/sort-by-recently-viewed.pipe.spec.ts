import { SortByRecentlyViewedPipe } from './sort-by-recently-viewed.pipe';

describe('SortByRecentlyViewedPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByRecentlyViewedPipe();
    expect(pipe).toBeTruthy();
  });
});
